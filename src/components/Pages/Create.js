import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({}));
const Create = () => {
  const classes = useStyles();
  return <div className={classes.createContainer}>Create</div>;
};

export default Create;
